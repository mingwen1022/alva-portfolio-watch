# Playbook 介绍

[English](Playbook-overview.md)

Skill 生成的 Playbook 由四个 tab、四个定时任务和一套告警投递规则组成。
本文说明各部分承担什么、告警如何判定与投递。信号的完整定义在
[`product/signal-spec.md`](product/signal-spec.md)，界面的逐块规格在
[`product/content-spec.md`](product/content-spec.md)。

---

## 四个 tab

按「这是什么」划分，不按投递层级划分。投递层级回答的是「送到哪个接触点」，
与页面结构是两个维度：能推送的一定在首屏，首屏的不一定推送。

| Tab | 内容 | 划分依据 |
|---|---|---|
| **1 · 持仓与告警** | 告警流 · 今日扫描 · 资产总览 · 三个切面的 Allocation · 净值曲线 · 持仓表 · 新闻与财报日历 | 已验证过的内容，可以占首屏 |
| **2 · 标的详情与告警历史** | 切换单只标的，看它的两年 K 线（标记历史触发）· 内部人申报 · 财报 · 相关新闻 | 客观事实，逐标的组织 |
| **3 · 市场** | 指数 · 波动率 · 利率 · 商品 · 加密情绪 · 全市场财报日历 | 客观数据，**不作为信号源** |
| **4 · 方法** | 什么算异动 · 什么被证伪 · 怎么排序 · 能力边界 | 判断依据与它的边界 |

Tab 3 的内容不参与任何触发判定。它存在的理由是让读者知道当天的大盘背景，
而不是让页面多一个信号来源。宏观族（MA1 · MA2）在回测中已被证伪，
把市场数据摆成信号会与那个结论矛盾。

Tab 1 在零告警的日子仍然显示当天的扫描读数。**没有告警与没有运行是两种状态**，
扫描表是引擎跑过的唯一证据。

---

## 什么算异动

价格与成交量必须在同一个交易时段都越过该标的自己的线。只有一条越线不构成告警。

| 信号 | 价格条件 | 量能条件 |
|---|---|---|
| **PV1 · 日线** | 稳健 z 绝对值 ≥ 1.5 | 量比 ≥ 2.0（美股）/ 3.0（加密） |
| **PV5 · 盘中 15 分钟** | 同时刻 z 绝对值 ≥ 4.75（美股）/ 10.0（加密） | 量比 ≥ 2.0 / 3.0 |

线由该标的自己的历史决定：稳健 σ 取 90 个交易日，盘中取同一时刻（如每天的 09:45）
过去 90 个样本。同一个百分比在不同标的上不是同一件事。1.5% 对 KO 是异动，对 DOGE 在噪音范围内。

阈值是固定值而非滚动分位。滚动分位会让「异动」的定义随市场自身漂移，
在高波动期把真实异动判成常态。

采用 AND 而非官方推荐的 OR，依据是触发量而非效果差异：等触发数条件下两者打平
（p = 0.70–0.84），而 OR 的触发日是 AND 的 5.96 倍，5 只标的的组合每周 5.6 次对 1.0 次。
该依据在切半检验的六种口径下全部翻号，因此证据等级封顶为「待验证」。

---

## 什么是噪音

判据只有一条：

```
触发后 5 个交易日的已实现波动 ÷ 平时同长窗口的中位数  →  相对基准倍数
95% 自助区间下界 > 1.0   ∧   独立块数 ≥ 5
```

逐标的计算，禁止跨标的池化。触发频率、幅度、命中率均不参与判据，它们是描述量。

按此判据测过 21 条告警候选，通过 1 条：

| 结论 | 信号 |
|---|---|
| 通过 | PV1 |
| 干净证伪 | MA1 0/16 · MA2 0/16 · EV2 0/56 · EV5 1/44 · DR3 无剂量反应 |
| 效应真实但太小 | EV3 中位倍数 1.117，PV1 为 1.49，单标的不可用 |
| 样本不足或不可复用 | DR1（阈值跨标的差 6 倍）· DR2 · EV1 |
| 数据阻断 | EV4 日历深度 1.6 年 · MA3 无 FOMC 日历端点 |

被证伪的信号不进入界面，也不留在告警流里。它们的实验记录保存在
[`backtest/signal-registry.md`](backtest/signal-registry.md)。

---

## 多个信号同时出现时的排序

不按信号类型排序，按投递上限排序。每条告警经过三道上限，取最严格的一道：

| 上限 | 含义 |
|---|---|
| `symbol_grade` | 这条规则在这只标的上验证过没有 |
| `degraded` | 这只标的是否处于高波动状态 |
| `signal_evidence` | 这条信号本身的证据等级 |

结果映射到四个投递层级：

```
L1   推送手机
L2   概览信号流
L3   持仓页
L4   记录页
```

**评级缺失按封顶处理**，不按通过处理。没有评级意味着没人测过这条规则在这只标的上是否成立，
向外失败会让未经验证的信号直接进入推送。

界面上，投递受限的标的在图标左侧显示一个点，浮窗说明是哪条规则受限、原因是什么。
「没有点」表示这只标的的价量规则都可以推送。

### 用户自己设定的线不受上限约束

止损（US1）、止盈（US2）、回撤（US3）三条由用户设定的线**豁免全部三道上限**，
始终按 L1 推送。它们是用户的指令，不是系统的判断。系统对某只标的的证据不足，
不构成压下用户指令的理由。

---

## 四个定时任务

| 任务 | 频率 | 产出 |
|---|---|---|
| 日线 | 每交易日收盘后 | PV1 · 扫描读数 · 持仓估值 · 净值曲线续接 |
| 盘中 | 每 15 分钟 | PV5 · 用户线 · 盘中价格刷新 |
| 盘前 | 每交易日开盘前 | 新闻 · 财报日历 · 内部人申报 · 资金费率 |
| 市场 | 每小时 | 指数 · 利率 · 商品 · 加密情绪 |

初始化脚本单独运行一次，产出基线、逐标的评级、历史触发回放与页面所需的九个文件。
基线属于初始化，运行期只取当天数据。阈值在基线建成时锁定，此后不再重解。

---

## 告警详情与可解释性

告警流里的每一条都可以点开成一张详情弹窗，把这条告警的判断依据摊在一处：为什么值得推送
（未达推送线的写明为什么没推）、触发时点、围绕触发那根 K 线的行情图并标出触发位置、
同期大盘对比、这次幅度在该标的历史上的分位、以及它在你持仓里的权重与盈亏。
弹窗内可以直接翻到上一条或下一条，不必退回列表。

进入 L1 的告警会附带一段归因：调用带检索的模型，在时间窗内匹配的新闻中寻找可能的解释。

归因的边界写在告警卡上：

- 它给出的是一种**可能的读法**，不是因果断言
- 匹配不到来源时显示「问过了，没找到」，而不是留空。「没问过」与「问了没找到」是两种状态
- 每日调用有额度上限，超出后的告警仍带来源与计算上下文，但没有文字段落
- 归因从不预测方向，也不给出交易建议

---

## 界面上的降级表达

| 情况 | 表达 |
|---|---|
| 未连接账户 | 金额、盈亏、净值曲线整块不显示，并说明这些需要账户；告警不受影响 |
| 基线不足 60 个交易日 | 该标的不出读数，显示「上市第 N 天」而非硬算一个数 |
| 未验证的资产类别（ETF 等） | 标记 `unvalidated`，阈值来自同类池反解，证据等级不得显示为绿 |
| 某个 producer 未运行 | 对应板块显示缺口说明，而不是空白。空白与「今天没有」无法区分 |
