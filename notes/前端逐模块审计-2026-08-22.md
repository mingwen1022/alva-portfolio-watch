# 前端待修清单 · 2026-08-22 逐模块点击审计

> **✅ 全部 26 条已修并逐条在浏览器里验过**（前端 session 中断后由本 session 接手）。
> 本文保留为记录：每一条的症状与根因，以及「38 条页面自检全绿而这些全都没被抓到」这个事实。

<sub>两个子 agent 驱动真实浏览器，五档账本 × 四个 Tab × 每个标的 × 每张弹窗。
⚠️ **页面自己的 38 条自检在所有状态下全绿，下面每一条都没被它们抓到。**</sub>

## P0 · 显示了错的数字

| # | 症状 | 根因 |
|---|---|---|
| 1 | 合并的盘中弹窗每行印同一个价格移动，符号还错 | `sigReads()` 走 `readingsAt(p,'bar')`，那是**当日最强那根**；z 与量比取自 finding 所以各不相同，唯独价格不是 |
| 2 | `NaN%` 印在 Portfolio value 的 crosshair 上 | `benchmark.points` 曾是空数组而那一行无条件渲染。**数据已补 502 点** |
| 3 | 财报日历印「in −93 days」 | `earnIn` 对 null 与负数没有措辞。**数据已把 `next` 恢复成 null** |
| 4 | 「触发」标记点开另一个粒度的告警 | `alertBySym` 取每标的第一条、不看粒度；标签来自看粒度的 `firedAt()` |
| 5 | 「Size in context」说契约没有排名 | 五条 bar finding 都有 `sizeRank`，页面读了 `of` 又断言字段不存在 |

**1 的细节** —— DOGE 15 分钟卡（标题 −2.6%）：

| 行 | 屏幕「价格 vs 线」 | 屏幕「偏离倍数」 | `measured.move` |
|---|---|---|---|
| 05:00 | **+3.3%** | −14.62 | **−2.59%** |
| 16:30 | **+3.3%** | +11.15 | **+1.44%** |
| 17:30 | +3.3% ✓ | +18.63 | +3.29% ✓ |

⚠️ **5 的补充**：六条 PV5 里四条是 `rank 1`，那是对的 ——
排名是**同一时刻**内的，09:00 与 21:30 是不同时刻，各自都能排第一。
**但文案必须点出时刻**（读 `trigger.barSlot`），否则读起来像「两根都是最大的」。

## P0 · 说了与数据相反的话

| # | 症状 |
|---|---|
| 6 | Tab 4 写「`degraded` 每个基线都是 null」，而同一次调用读到 AMD/MSTR/SOUN 三只 `"high_vol"`。硬编码字符串 |
| 7 | 「By theme」从不按账本重算 —— 加密only 里列着五只美股，合计 $46.8K 而账本只有 $18.4K |
| 8 | Tab 3 加密行标着卡片自己说没有的数（总市值 / 占比） |
| 9 | 「BTC is today's case, in the row below」指向**上方** |
| 10 | 新闻计数分子分母同源「12 of 12」。**数据已修**：`scanned = {holdings 8, newsItems 35, newsPassed 32}`，`news.json` 按 URL 去重 |
| 11 | Portfolio value 图不跟账本走 —— 美股only 的 KPI 说已重算，图画的是整本八只 |
| 12 | Coverage 印 `kline.length` 而不是 `baselineDays`（SPY 印 260，实际 783）；60 根那道门也从 `L.n` 重推，没读 `usable` |

⚠️ **7 在四种状态下 `auditTotalsAddUp()` 全绿。**

## P1

```
13 中文资金费率卡乱码        '὾0' 是五位十六进制 → JS 读成 ὾ + 字面 0
                          要 \u{1F7E0} 或代理对。英文那句根本没有徽章，两语言不一致
14 内部人脚注把 buys.filings 归到 30 天窗口  两个数的窗口不同，不能并列在一句里
15 资金费率卡假设 points 升序  端点本身返回倒序，没有排序也没有检查
16 图上标记 tooltip 印三个破折号  **数据已修** —— alertHistory 带 move/rvol/priceLine/volLine
17 亚元价格 2 位小数         DOGE 印 $0.09，从印出来的数算出的百分比与卡上对不上
18 内部人零买入状态同一件事说两遍
19 alerts in column 下扫描表没有明确的触发标记  content-spec 明令不能只靠「两根条都是琥珀」
20 零告警 + 价格陈旧         「两只已过两条线」被陈旧提示整句替换，而表里仍有两行琥珀
21 「3 alerts today」把持续中的也算进「今天」
22 1 positions · One alert cleared on their own  单复数
23 console: missing copy key: tipDR1 ×12
24 弹窗翻页顺序 ≠ 屏幕上的卡片顺序
25 加密only 仍渲染整个财报块（含 2.58× 财报日波动那段）
26 盘中图 x 轴是 UTC 而标题是 ET，轴上没有时区标注
```

## 审计确认干净的

KPI 行（含三档子集重算）· 持仓表读数（两个粒度 × 八只全部对上 `scan[]`）·
By holding 与 By asset class · Portfolio value 八种切换组合 ·
弹窗块构成 vs content-spec §四（含 GLD 的「为什么没响」）· tooltip 定位（滚动后仍 8px）·
四种布局的读数与文案一致 · Tab 2 蜡烛（14 只全有 OHLC）· 覆盖卡的 `insufficient_sample` 两道门分说 ·
ρ 只读不算 · 按键在不在决定区块 · 新闻空态 · Tab 3 裁剪与 null 省略 · Tab 4 认不出的键照样显示
