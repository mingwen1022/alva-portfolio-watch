# Portfolio Watch 解题思路

要求：构建一个 Skill，让任意 Alva 用户用一句话生成一个带界面和告警的 Portfolio Watch Playbook。明确交给 Skill 决定的是三件事：什么算异动、什么是噪音、多个信号并发时怎么排序。 三件都是告警问题，界面是它们的显示面。评分的核心判据是可复用性，即对一个从未见过的持仓 同样成立。

## 一、开发工具

Claude 承担信号研究、回测、页面开发与 Skill 构建，Alva 提供数据端点、脚本运行时与部署环境，
codex 只用在 eval 一处。eval 要回答的是「把 SKILL.md 交给一个不知道构建过程的 agent，
它能不能建出正确的东西」，用参与过构建的同一个模型来验，验的是记忆而不是文档，
因此这一环换成独立工具与独立账号。

## 二、平台调研与信号范围圈定

先摸清平台边界。探索 Alva Playbook 的对象模型、官方 alva/skills 的实现方式、CLI 能力与
计费口径，据此确定要构建的 Playbook 具体形态：四个 tab 的看板、四个定时任务、
一套推送到手机的告警。

告警范围圈定为美股与加密。这两类在 Alva 端点上有足够深度的历史数据，美股日线 2,168 根、
加密 3,152 根，2018 年 1 月起，足以支撑逐标的的统计验证。其他资产类别可以展示与告警，
但无法在同类资产上验证告警之后是否真的伴随波动。

在这个范围内调研业界通用的告警方法、指标与基准，圈出初始候选 28 条，覆盖价量、事件、
衍生品、宏观、政策语料、组合、用户规则七个族，逐条标注血统与它要验证的主张，
作为回测的 candidate。候选清单与逐条的实验记录在
[`backtest/signal-registry.md`](backtest/signal-registry.md)。

## 三、告警逻辑的定义与回测验证

告警采用双确认：价格与成交量必须在同一个交易时段都越过该标的自己的线，只有一条越线不构成
告警。线由该标的自己的历史决定，不使用跨标的的统一阈值，因为同一个百分比在不同标的上不是
同一件事。日线用 PV1，盘中 15 分钟用 PV5。

验证的判据只有一条：触发之后 5 个交易日的已实现波动，除以平时同长窗口的中位数，
95% 自助区间下界大于 1.0。逐标的计算，禁止跨标的池化。触发频率、幅度、命中率都不进判据，
它们是描述量。

按此判据测过 21 条告警候选，通过 1 条。宏观族与事件族的多条被干净证伪，几条效应真实但小到
单标的不可用，几条样本不足或阈值跨标的相差数倍因而不可复用。被证伪的信号不进入界面，
也不留在告警流里。28 条候选最终定案 13 条。

分级不按信号类型，按投递上限。每条告警经过三道上限并取最严格的一道：这条规则在这只标的上
验证过没有、这只标的是否处于高波动状态、这条信号本身的证据等级。结果映射到四个层级，
L1 推送手机，L2 进概览信号流，L3 只在持仓页，L4 只进记录页。评级缺失按封顶处理而非按通过
处理，方向是向外失败。用户自己设定的止损、止盈、回撤三条线豁免全部三道上限，
它们是用户的指令而不是系统的判断。

每条告警都能点开成一张详情弹窗，把判断依据摊在一处：为什么值得推送、触发时点、
围绕触发那根 K 线的行情、大盘对比、幅度分位与持仓权重，用户可以自己复核这条告警。
进入手机推送的告警还附带一段归因，用带检索的模型在时间窗内匹配到的新闻里找一段可能的
解释，让用户看到这条告警为什么现在响。分工是明确的：越线判定、逐标的评级、投递层级
全部由确定性代码完成，LLM 只在告警已经产生之后被调用，不参与「是否构成告警」的判断。
归因的边界写在告警卡上：它给出的是一种可能的读法而不是因果断言，匹配不到来源时显示
「问过了，没找到」而不是留空，从不预测方向，也不给出交易建议。

被降级的信号不会消失，而是留在页面上。财报临近、内部人申报、资金费率这类没通过判据但事实
明确的内容进入持仓页与记录页，用户想看时能看到，但不会在手机上响。信号的完整定义在
[`product/signal-spec.md`](product/signal-spec.md)，回测依据在 [`backtest/`](backtest/)。

## 四、数据管线与数据契约

管线分四段：取数、M 层指标、S 层信号、告警引擎。M 层每个量只定义一次并被多条信号引用，
S 层只引用 M 的 ID，不重复定义算式。这个结构是为了让改一次阈值只改一处。

管线的产物落成九个文件，构成前端与脚本之间的数据契约。契约明确区分三种状态：有值、值为空、
字段不存在。三者在页面上的表现必须不同，否则「上游没给数」与「今天没有」会长得一样，
排查会被带向错误的一层。

链路全图在 [`product/data-pipeline.md`](product/data-pipeline.md)，契约在
[`product/output-schema.md`](product/output-schema.md)。

## 五、页面设计与本地联调

页面按内容划分四个 tab，不按投递层级划分。投递层级回答的是送到哪个接触点，与页面结构是两个
维度。第一个 tab 是持仓与告警，第二个是单标的详情与告警历史，第三个是市场背景，
第四个是方法与边界。市场 tab 的数据不参与任何触发判定，它存在的理由是提供当天的大盘背景；
宏观族已在回测中被证伪，把市场数据摆成信号会与那个结论矛盾。

页面先在本地接真实数据调试，逐块核对每一格的数值来源与算式。逐块规格在
[`product/content-spec.md`](product/content-spec.md)。

## 六、部署到 Alva 与生产验证

将 Playbook 部署到 Alva，运行数据管线，配置四个 cron job 与 automation，接通手机告警，
然后验证实际部署后的执行与本地是否一致。

这一步暴露的问题都不是逻辑问题，而是平台行为与本地假设不符。写入文件系统不等于发布，
发布页是版本化快照；KV 按 feed 路径隔离而 feed 路径是常量，同一个 Skill 建出的多个 Playbook
会共用 KV；发布页在网关后，相对路径取数全部失败。这些只有真部署才会出现，
本地跑通不能替代。

## 七、Skill 提取

按单次构建走通的链路，把最终选定的数据源、指标、告警规则、数据管线、schema 与前端逻辑
提取成 SKILL.md、references、scripts 与前端 template。

SKILL.md 的读者是要去建 Playbook 的 agent，不是评审，因此它只写祈使句与决策树，
不承载论证，论证留在 [`product/`](product/) 与 [`backtest/`](backtest/)。
references 与 product 的规格同源，避免两处各写一份。

## 八、端到端 eval

评估方式是把 SKILL.md 交给一个不知情的 agent，在隔离环境里真跑，然后只验产物。隔离是三重的：
空目录、独立的 codex 配置、独立的 Alva 账号。

query 按输入形态切分而非按信号切分，因为 Skill 面对的差异来自用户怎么说、持仓长什么样，
而不是哪条信号会响。覆盖的形态包括混合持仓、单标的、纯加密、ETF 与新上市标的、港股、
交互式探针、把持仓直接写进 query，以及一次触发演练。其中三轮的正确结果是不产出，
用来检验 agent 会不会在账户授权失效或端点无数据时硬造一个结果。

共 10 个案例、17 轮真跑。判官分六层，能用代码判的一律不交给模型，只有最后一层需要判断的
交给多个子 agent 分角度投票。累计记录 60 条 badcase，逐条定向修复后重跑，
直到该案例的全部断言通过。台账里除了「修了没有」还有一列「判官当时抓到没有」，
60 条里判官当时漏了 41 条，这一列比前一列更值得读，因为缺陷会被修掉，
而判官的盲区不修就会一直在。案例、判官与台账在 [`eval/`](eval/)。

## 九、边界

真实券商账户的接入路径未经端到端验证。接口的字段名与嵌套形状已确认，未验证的是多币种账户、
SHORT 持仓的盈亏符号、保证金账户的权重口径、券商快照与 kline 价格的偏离幅度、
刷新频率与延迟。linked=true 分支需要一个含多笔、含空头、非单一币种的账户重新调试。

构建周期 5 天，全部测试使用模拟数据。盘中、盘后与跨周的实际执行未经周期性验证。
正常上线应经过一段灰度，验证所有逻辑在真实时序下的执行情况。

eval 的覆盖有限。每一轮真跑需要 agent 完整走完构建链路，出于时间与成本考虑共 10 个案例、
17 轮。实际产品应覆盖更多场景并重复多次，以区分「这一轮对了」与「这个 Skill 稳定」。

回测只覆盖美股与加密。ETF、港股等类别在有历史数据时可以正常展示与告警，
但告警之后是否伴随量价波动未在同类资产上验证。